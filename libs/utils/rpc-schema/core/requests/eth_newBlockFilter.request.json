{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id":"eth_newBlockFilter.request.json",
    "title": "eth_newBlockFilter request parameters",
    "description": "Creates a filter in the node, to notify when a new block arrives.",
    "$comment":"To check if the state has changed, call `eth_getFilterChanges`.",


    "additionalItems": false,
    "items": [
      {
        "$ref":"type.json#/definitions/QUANTITY",
        "description":"a filter id"
      }
    ],
    "minItems": 1,
    "maxItems": 1,

    "examples": [
      []
	 ]
}
