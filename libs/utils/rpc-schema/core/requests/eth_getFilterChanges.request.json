{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id":"eth_getFilterChanges.request.json",
    "title": "eth_getFilterChanges request parameters",
    "description": "Polling method for filter, which returns an array of logs which occurred since last poll",

    "additionalItems": false,
    "items":[
      {
        "$ref":"type.json#/definitions/QUANTITY",
        "description":"the filter id."
      }
    ],

    "maxItems": 1,

    "examples": [
      ["0x0"],
      ["0x3a"]
	]
}
