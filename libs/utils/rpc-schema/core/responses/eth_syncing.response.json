{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id":"eth_syncing.response.json",
    "title": "eth_syncing response results",
    "description": "Returns an object with data about the sync status or `false`.",

    "oneOf":[
      {
        "type": "boolean",
        "enum":[false],
        "description":"`false`, when not syncing"
      },{
        "type":"object",
        "properties":{
          "startingBlock":{
            "$ref":"type.json#/definitions/QUANTITY",
            "description":"The block at which the import started(will only be reset, after the sync reached its head)"
          },
          "currentBlock":{
            "$ref":"type.json#/definitions/QUANTITY",
            "description":"The current block, same as eth_blockNumber"
          },
          "highestBlock":{
            "$ref":"type.json#/definitions/QUANTITY",
            "description":"The estimated highest block of the network(0 if no peer)."
          }
        }
      }
    ],

    "examples": [
      false,
      {
        "startingBlock": "0x384",
        "currentBlock": "0x386",
        "highestBlock": "0x454"
      }
	]

}
